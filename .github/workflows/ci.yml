name: Who tests the tester
on: [pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          echo '# Hello there
          Would bob have done what he did, had he known what would
          happen to him? <video></video>' > $GITHUB_STEP_SUMMARY



